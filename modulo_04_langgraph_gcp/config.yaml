# ═══════════════════════════════════════════════════════════════════
#  modulo_04_langgraph_gcp — Configuración central del módulo
# ═══════════════════════════════════════════════════════════════════

# ── Modelo LLM principal ──────────────────────────────────────────
# gemini-2.5-flash-lite — rápido, económico, ideal para demos.
# Alternativas:
#   gemini-2.5-flash  — más capaz, misma velocidad
#   gemini-2.5-pro    — más potente, requiere más tiempo y costo
#
# NOTA: thinking_budget NO se usa en este módulo porque usamos
# ChatVertexAI (LangChain), que no expone ese parámetro directamente.
# Solo es relevante si usas google-genai SDK (modulo_03_gcp).
model:
  name: gemini-2.5-flash-lite
  location: us-central1
  temperature: 0.3
  max_output_tokens: 3000

# ── Modelo de embeddings ──────────────────────────────────────────
embedding:
  model: text-embedding-004   # Modelo estable de Vertex AI

# ── Configuración RAG ─────────────────────────────────────────────
rag:
  # Ruta relativa desde modulo_04_langgraph_gcp/ al ChromaDB de modulo_03
  # Este ChromaDB fue construido en el módulo anterior con 302 vectores
  chroma_path: ../modulo_03_gcp/data/chroma_db
  collection: mily_una_noches
  top_k: 5

# ── Multi-Agente ──────────────────────────────────────────────────
multi_agent:
  max_iterations: 6   # Anti-loop: máximo de ciclos en el grafo
